# Common Commands:

show installed packages:
```
pip list
```

Check available models:
curl -L https://models.inference.ai.azure.com/models \
  -H "Authorization: Bearer YOUR_GITHUB_TOKEN" \
  -H "Content-Type: application/json"
